# Legacy Frontend

This directory contains the original lightweight frontend kept for historical comparison.
The active frontend is `../web1.0`; new UI work and manual testing must use that directory.
